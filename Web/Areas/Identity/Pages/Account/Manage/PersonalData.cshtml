@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Datos Personas";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
    Layout = "/Views/Shared/_configLayout.cshtml";
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Su cuenta contiene datos personales que nos ha proporcionado. Esta página le permite descargar o eliminar esos datos.</p>
        <p>
            <strong>Eliminar estos datos eliminará permanentemente su cuenta y no se podrá recuperar.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-success" type="submit">Descargar</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-success">Eliminar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}